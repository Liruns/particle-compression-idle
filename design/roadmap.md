# 제작 로드맵 (roadmap v0.2)

- 작성: game-director (총괄)
- 작성일: 2026-06-25 · 갱신: 2026-06-26 (우주적 현미경 피벗 + 정거장2 진행 반영)
- 입력: `design/scope-mvp.md`(4단계 게이트) + `design/tech-architecture.md`(기술 결정) + GDD §13 + `cosmic-direction.md`(비주얼/UI 피벗).
- 상태: **확정 로드맵 v0.2.** scope-mvp 4단계를 척추로 한 제작 순서·마일스톤·졸업 기준. **v0.2 = "AI 느낌" 피드백 → 우주적 현미경 피벗 반영, 정거장2 진행 갱신.**
- 가정: **비전문 1인 ~ 소규모 팀.** 기간(주/월)은 변동이 크므로 **상대 규모(작음/중간/큼)**로 표기. "지금 어디고 다음이 뭔지"를 한눈에.

> ## 📍 지금 어디 (2026-07-08 갱신)
> **정거장1(프로토타입) 졸업 → 정거장2(수직 슬라이스) 진행 중.**
> - **피벗 발생:** 기존 다크 대시보드가 오너에게 "AI 느낌"(무난한 선택의 합=무개성)으로 읽힘 → **"우주적 현미경"으로 전면 피벗.** 프로토타입(`prototypes/cosmic-layers.html`·`cosmic-particle-game.html`)으로 방향 확정. 통합 SSOT = `cosmic-direction.md`, 비주얼 권위 = `art-direction-cosmic.md`. **로직·경제·밸런스는 한 줄도 불변.**
> - **이관 1단계 ✅ 완료:** 렌더 레이어를 11 우주 세계로 교체(`src/render/layer-worlds.ts`·`world-renderer.ts`), 게임 현재 층으로 구동, **그린(check 0 / test 254)**. 로직·`App.svelte` 불변.
> - **이관 2단계 ✅ 완료:** 대시보드 UI → **다이제틱 공허 게임판 + 입자 상호작용**(`src/render/board.ts` `BoardRenderer` + `App.svelte` 재구성). 자원=성표 주석(DOM), 체인=궤도 껍질, 능동=세포 만지기→`manualCompress()`, 껍질 클릭→`buy()`. 디바이스=가장자리 bloom 노드. **그린(check 0 / test 254)**, `game.ts`·`data/`·`core/` 0줄 변경. 상세 = `cosmic-direction.md` §3·§4·§7-B.
> - **사운드 1차+2차 ✅:** 절차적 WebAudio(에셋 0, `src/core/audio/`). **1차** = 이벤트 버스 구독 SFX(압축·결속·공명·위상·하모닉·연구·발견·상전이·오프라인·업적, 층 색온도↔음역, 침묵 기반). **2차** = 층별 앰비언트 베드(`ambient.ts` — 드론+필터 노이즈, 웜↔쿨 아크, 층 하강 크로스페이드) + 앰비언트 on/off 설정. 첫 제스처 unlock. **로직·경제 0줄(읽기 전용).** 상세 = `audio-design.md` §7.
> - **설정/접근성 ✅ 완료:** 공허 dock에 **설정** 노드 추가 → 사운드(뮤트·볼륨)·모션(자동/감소, OS 감소 우선 §accessibility 2-B)·숫자 표기(과학/공학/표준)·세이브 데이터(내보내기·가져오기·초기화). 죽어있던 `SettingsState.notation`을 UI로 연결(표시 전역, callsite 0 수정) + F1 export/import를 UI에 노출. 기기 취향(사운드·모션)=localStorage 분리, 진행 취향(표기법)=세이브 보존.
> - **기록(통계) ✅ 완료:** 공허 dock **기록** 노드 → 누적 통계(플레이타임·가장 깊이 dec/반경·최대 층·누적 C/D·수동 압축·결속 수·연구/상전이/재하강 횟수). `StatsState`(옵셔널 세이브 필드, 버전 업 없이 보존 — 구버전=0 기본, 상전이/재하강 넘어 전 생애 누적).
> - **온보딩 마찰 수정 ✅:** 물 분자 t=0 자동 발견으로 FTUE stage가 즉시 'codex'로 뛰어 "결속하라"가 살 수 없는 초반(첫 T1 ≈ 439E) 내내 뜨던 버그 → **힌트를 액션 신호 기반**으로(살 수 없으면 압축/살 수 있으면 결속/구매 후 가속). 살 수 없는 최적 껍질의 넛지 비드도 끔(모순 회피). (`ftue`·`board`)
> - **관측 목표(업적) ✅ 완료:** 공허 dock **목표** 노드 → 26개 목표(첫걸음·하강·상전이·도감·연구·손맛·스케일 + 히든 2). 영속 상태 파생 판정(멱등), `AchievementsState`(옵셔널 세이브, 구버전=빈 집합). **순수 인정·목표형 — 생산 배율 0**(§13 가드레일: 마일스톤 배율 금지 준수, 경제 불변). 달성 토스트+사운드, 부팅/로드 catch-up은 억제. Steam 업적(M3.7) 매핑 후보. (부수: 온보딩 첫 승리 채움 / 오디오 layerEnter 이중구독 버그 수정.)
> - **내러티브 보이스(M2.7 부분) ✅:** 상전이 비트를 **미지 6 서브층 전부(prestigeIndex 1~6)**로 완성(`data/narrative.ts` — 프리온·끈·루프·거품·정보·플랑크, narrative.md §4-2 비트 5~10 정본). 발견 아크(확인→경이→충격→침묵→붕괴)·색온도 아크 정합, 느낌표 절제(빅 크런치 1회 예약). **버그 수정: 층 진입 비트 "스케줄"→"스케일" 오타 4곳.** 잔여 = 빅 크런치(PT7) 3단 시퀀스·시그니처 로그·재하강 회차 변주(`followup-bigcrunch-framing.md`).
> - **관측 안내(도움말) + 키보드 ✅ (M2.6 부분):** 비관습적 다이제틱 공허 UI의 **조작 발견성** 보강 — 상설 참조 패널(`HelpView`, dock **안내** 노드/`?` 키): 조작·키보드·자원·능동 메커니즘 설명. 키보드: **스페이스=압축**, 1~4=결속 수량, Esc=닫기, ?=안내(텍스트 입력 중엔 억제). 능동 메커니즘 다이제틱화는 이미 완료(오비탈=공전 전자 직접 클릭·위상 노드·하모닉 passive). 로직 0 터치.
> - **첫 상전이 머니샷 ✅ (M2.5 부분):** 전환 하강("떨어지며 이어짐")은 이미 있었음. **첫 미지 진입(쿼크→프리온)** 만 구별되게 — 더 긴 하강(3.2s vs 1.7s) + 깊은 문턱 어둠(0.82 vs 0.55) + 도착 시 냉색(프리온) 블룸(플래시 아님). App이 "미지 층 첫 진입"만 머니샷 신호(로드 세이브 재방문 제외). 색온도 아크는 기존 색 보간이 담당(`WorldRenderer`). 잔여 = 빅 크런치 연출.
> - **토스트 우선순위 cap ✅ (게임필):** 상전이 순간 서사 비트(legendary)가 업적 토스트(discover) 다발에 밀려나던 버그 수정 — `toast-priority.ts` 순수 cap(legendary>beat>discover, 동률 최신 우선, 표시는 삽입 순). 상전이 머니 모먼트 텍스트 보존.
> - **버그·강건성 ✅:** (1) 업적 `prestige_7`이 `prestigeCount>=7`이라 **영원히 달성 불가**였음(prestigeCount는 WALLS 6개라 최대 6) → `prestige_6`(프리온~플랑크 전부, ≥6)로 재정의, 미구현 재하강 업적 `redescent_1`은 hidden 처리(완성도 26 목표 유지). (2) 세이브 신규 필드(stats·achievements) **전체 봉투 파이프라인**(checksum·구버전 호환·export/import·어댑터 save/load) 강건성 테스트 보강.
> - **★ 빅 크런치(PT7) + 재하강 코어 ✅ (M3.2~3.3 착수):** economy.md 정독으로 선결 4건을 확정·구현. ① `prestige.count` = **회차 내 벽 카운터**(빅 크런치 시 0 리셋 → 검증된 `nextPrestigeIndex` 무변경·6벽 재통과), 빅 크런치 횟수는 `runIndex`. ② D_current 보존 = `dPreservation(newRunIndex)` 곡선 `[1,.65,.50,.40,.40,.38,.35]`(§7.3, race 무영향). ③ 벽 재트리거 = ①로 자동. ④ UI = PrestigeView 재하강 모드(종착점 언어 금지 — "재압축·이전 관측 유지·더 작은 것") + 재하강 비트. **판정**(`isBigCrunchAvailable` = count≥6 && dec≥26)·**미리보기**(K=1.05)·**리셋**(`applyBigCrunchReset`: E·C·체인·층→분자·count→0·runIndex+1·QF 가산·D 회차보존) 전부 순수+테스트. 검증: 유닛 9(K=1.05 정합 1.084e7→1.138e7·보존곡선·리셋) + 통합 2(game.executeBigCrunch 상태 반영). **경제 수치 0 변경 — 설계 이관.** 잔여: 빅 크런치 3단 블랙아웃 시퀀스·집중 서브층 로테이션·심화 콘텐츠(M4).
> - **이관 3단계 ▶ 다음:** 게임필 마감·전환 폴리시 미세조정. 재하강 심화(집중 서브층 회전·심화 입자/노드) = 콘텐츠 후속(M4).

---

## 0. 이 문서를 읽는 법 (문외한용)

게임은 **한 번에 다 만들지 않는다.** 작게 만들어 "재미있나" 확인하고 → 한 구간을 예쁘게 다듬어 "팔리겠나" 확인하고 → 전체를 완성해 출시하고 → 라이브로 키운다. 이 순서가 **로드맵의 척추**이고, scope-mvp.md가 정한 4단계가 그대로 4개의 큰 정거장이다.

```
   [지금]
     │
  ┌──▼─────────┐   ┌──────────────┐   ┌──────────┐   ┌───────────┐
  │ 1. 프로토타입 │──▶│ 2. 수직 슬라이스 │──▶│ 3. v1.0   │──▶│ 4. 출시 후 │
  │ "재미있나?"  │   │ "팔리겠나?"     │   │ "완성"    │   │ "키운다"   │
  └────────────┘   └──────────────┘   └──────────┘   └───────────┘
   재미 검증         스토어 GIF          Steam 출시      라이브 콘텐츠
```

각 정거장에는 **졸업 기준**이 있다. 그걸 통과해야 다음으로 간다. 통과 못 하면 — 다음으로 가지 말고 **그 자리에서 고친다.** 게이트의 존재 이유가 그것이다(특히 정거장 1).

**핵심 원칙 하나:** 정거장 1에서 만드는 **기술 코어(저장·큰 수·게임 루프)는 모든 정거장이 재사용한다.** 그래서 1일차부터 제대로 짜야 하고, 그게 이 로드맵에서 가장 중요한 "한 번만 제대로" 구간이다.

---

## 1. 정거장 1 — 프로토타입 ("코어 루프가 재미있나?")

> **목표:** 못생긴 채로, 압축→작아짐→강해짐의 루프가 손에서 돌고 첫 상전이가 "또 하고 싶다"를 만드는지 확인. **남에게 보여주는 게 아니라 우리가 확인하는 단계.**

### 1-A. ★ 1일차부터 고정할 것 (tech-architecture 기반 — 나중 도입 비용이 큼)

이 4가지는 **프로토타입 첫날 코드 골격에 박아넣는다.** 나중에 추가하면 이미 쌓인 세이브·코드를 깨야 한다(tech-architecture §1.2·§7 후속 메모 명시). 프로토타입이 "임시"라도 이 뼈대만은 영구적이다.

| # | 1일차 고정 | 왜 지금 (안 하면) | 출처 |
|---|---|---|---|
| F1 | **세이브 봉투 포맷 `{version, data, checksum}`** | 나중에 `version`을 추가하면 그 전 세이브들은 마이그레이션 불가 → 영영 못 읽음 | tech-arch §1.2 |
| F2 | **StorageAdapter 추상화 (read/write/exists/backup)** | 웹 localStorage로 시작해도 인터페이스로 감싸야 Steam(fs) 이식 시 게임 코드 0줄 수정 | tech-arch §1.6 |
| F3 | **고정 timestep 게임 루프 (tick/render 분리)** | economy 시뮬이 dt=1~2s 결정적 가정. 나중에 바꾸면 검증된 페이싱이 실제 게임에서 안 맞음 | tech-arch §4.1 |
| F4 | **break_eternity.js 경계 (게임수치=Decimal, 카운트=number)** | break_infinity로 시작하면 재하강 누적 lifetime_C가 한계 돌파 → 교체 마이그레이션 비용. 처음부터 break_eternity | tech-arch §2.1·§2.2 |

> 이 4개를 "F-게이트"라 부른다. **F-게이트 없이 콘텐츠부터 만들기 시작하면 안 된다.** 이건 집의 기초공사다.

### 1-B. 마일스톤 (대략 순서)

| 순서 | 마일스톤 | 핵심 산출물 | 규모 |
|---|---|---|---|
| M1.1 | **F-게이트 + 빈 게임 루프** | 봉투 세이브·StorageAdapter·고정 timestep·Decimal 경계가 도는 빈 껍데기. 화면엔 숫자 하나 | 중간 (한 번만 제대로) |
| M1.2 | **8단 압축기 체인 + 코어 수식** | dec0~19 진행, base_k·growth·dec=α·log₁₀C, r↓+E↑. 자기증식 가속 | 중간 |
| M1.3 | **알려진 물리 온보딩 (dec0~9)** | 분자→원자→핵→핵자→쿼크 무상전이 층 진입 + "층 진입 ≠ 상전이" | 작음 (수치 대부분 확정) |
| M1.4 | **능동 손맛: 수동 압축 + 오비탈 공명** | 클릭 피드백, 공명 슬롯, 60/40 룰 | 작음~중간 |
| M1.5 | **첫 상전이 (PT1, dec19, 프리온 진입)** | QF 획득 → production_mult 부스트. "프레스티지의 무게" | 중간 |
| M1.6 | **미지 서브층 1~2개 (프리온 + 선택 끈)** | 위상 겹침 메커니즘 (+진동 하모닉스) | 중간 |
| M1.7 | **도감 일부 + 연구 1~2가지 + 오프라인** | 알려진 물리 ~15~25입자, 체인증폭 A가지 일부, 오프라인 크레딧(24h캡·0.65·탬퍼클램프) | 중간 |

- **순서 근거:** F-게이트(M1.1)가 모든 것의 토대 → 코어 엔진(M1.2)이 일의 대부분 → 온보딩·손맛(M1.3~4)이 첫 경험 → 첫 상전이(M1.5)가 클라이맥스 → 미지/도감/오프라인(M1.6~7)이 "방치형+미지"의 증거. 데이터 주도(tech-arch §4.4 `data/`)라 입자·노드 수치는 economy/content 확정분을 주입만.
- **병렬 가능:** M1.4(손맛)와 M1.7의 도감 데이터는 코어 엔진과 독립적이라 1인이라도 "코어 막힐 때 도감 작업" 식으로 전환 가능. 팀이면 분담.

### 1-C. 졸업 기준 (이게 충족돼야 정거장 2로)

scope-mvp §2 졸업 기준 그대로:
1. 첫 30초에 "작아짐=강해짐"이 화면에서 느껴진다.
2. 자동화 전 1~2분 능동 클릭이 지루하지 않다.
3. 첫 상전이가 "해냈다 + 또 하고 싶다"를 만든다.
4. 닫고 켜니 오프라인 진행이 기분 좋다.
5. **개발자 본인이 "한 판 더" 하고 싶다.** (가장 중요)

> ⚠️ **3·5가 안 나오면 콘텐츠를 더 만들 때가 아니라 코어 루프를 고칠 때다.** 여기서 멈추고 economy/systems를 다시 본다. qa-balancer 플레이테스트로 객관 확인 권장.

### 1-D. 의존성
- **선행:** 없음 (출발점). economy/systems/codex 확정 수치는 이미 존재 → 주입만.
- **이 단계가 막는 것:** 정거장 2·3 전부. 코어가 안 돌면 다음이 없다.

---

## 2. 정거장 2 — 수직 슬라이스 ("진짜 게임처럼 보이고 들리나?")

> **목표:** 한 구간(온보딩~첫 상전이 추천)을 비주얼·사운드·게임필까지 *완성도*로 다듬어 **스토어 GIF·트레일러 클립이 나오는 수준**으로. 폭이 아니라 깊이.

### 2-A. 마일스톤

> **피벗 반영:** 정거장2의 비주얼 방향은 **우주적 현미경**으로 확정됐다(`cosmic-direction.md`/`art-direction-cosmic.md`). M2.1 아트 방향은 **문서로 확정 완료** — 정거장2는 "그 비전을 실제 게임에 이관"하는 단계로, **이관 1단계(11 세계 렌더)는 이미 완료**됐고 **2단계(공허 게임판 UI)가 본 정거장의 핵심 작업**이다.

| 순서 | 마일스톤 | 핵심 산출물 | 규모 | 상태 |
|---|---|---|---|---|
| M2.1 | **아트 디렉션 확정** | "우주적 현미경" 비전 — 11층 distinct 세계·색온도 아크·탈채도 발광·관조↔공허 모델 (`art-direction-cosmic.md`) | 중간 | ✅ 문서 확정 |
| M2.2 | **이관 1단계 — 11 우주 세계 렌더** | `layer-worlds.ts`·`world-renderer.ts`, 현재 층 구동, 전환 하강. 로직 불변, check 0/test 254 | 중간 | ✅ 완료 |
| M2.3 | **★ 이관 2단계 — 다이제틱 공허 게임판 + 입자 상호작용** | 대시보드 폐기 → 자원=성표 주석, 8단 체인=궤도 껍질, 능동(압축·공명·위상·하모닉스)=떠다니는 입자 직접 만지기, 장치=어둠에서 피어나는 노드 (`cosmic-direction.md` §3·§4) | 큼 | ✅ 완료 |
| M2.4 | **사운드 1차 + 2차(앰비언트)** | 절차적 WebAudio(에셋 0): SFX 10종(이벤트 버스·층 색온도↔음역) + 층별 앰비언트 베드(`ambient.ts`, 웜↔쿨 4축 보간·층 크로스페이드·설정 토글). 첫 제스처 unlock·mute/volume/ambient(설정). | 중간 | ✅ 완료(1·2차) |
| M2.4b | **설정/접근성 콘솔** | 공허 dock 설정 노드 → 사운드·모션(자동/감소)·숫자 표기(과학/공학/표준)·세이브 export/import/초기화. `SettingsState.notation` UI 연결 + F1 export/import 노출. | 작음~중간 | ✅ 완료 |
| M2.4c | **기록(통계) + 온보딩 마찰 수정** | dock 기록 노드 → 전 생애 누적 통계(`StatsState`, 옵셔널 세이브). FTUE 힌트를 액션 신호 기반으로(살 수 없는데 "결속하라" 뜨던 버그 수정) + 넛지 비드 어포던스 정합. | 작음~중간 | ✅ 완료 |
| M2.4d | **관측 목표(업적) 시스템** | dock 목표 노드 → 26개 목표(`AchievementsState`, 옵셔널 세이브). 영속 상태 파생 판정·달성 토스트/사운드. **순수 인정형(생산 배율 0, §13 가드레일)** — Steam 업적(M3.7) 매핑 후보. 리서치 최상위 리텐션 갭 충당. | 작음~중간 | ✅ 완료 |
| M2.5 | **전환 하강 + 첫 상전이 머니샷 폴리시** | "떨어지며 이어짐" 연속 블렌드(플래시 금지) + 첫 상전이(쿼크→프리온) 색온도 식는 머니샷 — 더 긴 하강·깊은 문턱 어둠·냉색 블룸(art-cosmic §6). | 중간 | 부분 (전환·머니샷 완성, 빅 크런치 연출 잔여) |
| M2.6 | **FTUE 풀 폴리시 (첫 5분)** | 텍스트 벽 없이 점진 공개, 온보딩~첫 상전이 + **상설 관측 안내(HelpView)·키보드(스페이스 압축·? 안내)** 로 조작 발견성 보강. 공허 모델 위 ux.md/ui-flow.md *정보 로직* 보존. | 중간 | 부분 (힌트·안내·키보드) |
| M2.7 | **내러티브 보이스 + 게임필** | 층 전환 비트(L2~5) + 상전이 비트 6종(프리온~플랑크, narrative §4-2) 완성. 발견 아크·색온도 정합·느낌표 절제. 잔여 = 빅 크런치 3단 시퀀스·시그니처 로그·회차 변주. | 작음~중간 | 부분 (비트 완성) |

- **순서 근거:** 아트 방향(M2.1)·1단계 렌더(M2.2)는 **이미 완료** → **2단계 공허 게임판(M2.3)이 본 정거장의 본체**(대시보드→다이제틱 전환이 가장 큰 작업) → 사운드(M2.4)·전환 폴리시(M2.5)는 병렬 → FTUE(M2.6)가 첫 경험에 엮음 → 보이스·게임필(M2.7) 마감.
- **★ 핵심:** 정거장2 졸업 = 스토어 GIF인데, **머니샷이 "끝없는 하강·같은 화면 다른 세계·첫 문턱"**(art-cosmic §8)이다. 1단계 세계 렌더가 이미 그 GIF의 기반 → 2단계 공허 게임판이 완성하면 GIF가 나온다.
- **병렬:** audio-designer / graphics-programmer(전환 폴리시) / narrative-designer가 분리 레인 → **정거장 1 코어·경제 로직을 안 건드리므로 대부분 병렬.** game-programmer는 M2.3(공허 게임판)에 집중.
- **가드(피벗):** 모든 2단계 산출물은 `art-direction-cosmic.md` §7 금지목록 10개를 위반하면 반려(= AI 중앙값 회귀). economy/systems 수식·밸런스를 건드리면 반려. `cosmic-direction.md` §7-C 체크 통과.

### 2-B. 졸업 기준
1. 그 구간만 떼어 보여줘도 "완성된 게임" 같다.
2. **스토어용 GIF 3~5개가 이 슬라이스에서 나온다** (→ 정거장 3에서 위시리스트 시작점, scope §3 연결).
3. 압축 클릭·상전이 사운드가 "기분 좋다".
4. 첫 5분이 설명서 없이 이해된다.

### 2-C. 의존성
- **선행:** 정거장 1 졸업(코어 루프가 돈다). 폴리시할 *내용*이 있어야 폴리시한다.
- **산출의 쓰임:** 이 슬라이스의 GIF가 정거장 3의 **launch-plan 위시리스트 캠페인 출발점**(launch-plan.md §위시리스트).

---

## 3. 정거장 3 — v1.0 Steam 정식 출시 ("완성된 게임")

> **목표:** 온보딩부터 첫 빅 크런치를 지나 재하강 루프 안착까지, 4필러가 끝까지 작동하는 완전한 1차 사이클 + 반복의 입구. scope-mvp §4 IN 목록 전부.

### 3-A. 마일스톤 (대략 순서)

| 순서 | 마일스톤 | 핵심 산출물 | 규모 |
|---|---|---|---|
| M3.1 | **미지 서브층 4개 추가** | 루프(스핀네트워크)/거품(요동)/정보(홀로그래픽)/플랑크(픽셀붕괴). 6 미지 메커니즘 완성 | 큼 |
| M3.2 | **빅 크런치 (PT7) + "시작점" 프레이밍** | 플랑크 도달→최종 QF 폭발→재하강 입구. 종착점 언어 금지 | 중간 |
| M3.3 | **재하강 기본 루프 (1~2회)** | dec0 재시작 + lifetime_C·QF 누적, 회전 집중 서브층 선택, 방치/개입 분리, D보존 회차곡선 + 심화 훅 씨앗 1~2개 | 큼 |
| M3.4 | **도감 87입자 완성 + 홀로그래픽 배율** | 실제 57 + 상상 30, 곡선 B(≤×1.35) | 큼 |
| M3.5 | **연구 트리 핵심 가지** | A.체인증폭(14)+C.자동화(10) 전체 + B/D 코어 노드 | 중간~큼 |
| M3.6 | **아트·사운드·내러티브 전체 확장** | 6층 팔레트, 미지 메커니즘 VFX, 재하강 연출(회차별 축소), 풀 보이스 | 큼 |
| M3.7 | **Tauri 패키징 + 위젯 + 인프라** | Tauri v2(투명·프레임리스·always-on-top 위젯 창)+steamworks-rs, Steam Cloud, 업적, lz-string 세이브 (오버레이 제외 — 위젯 불요) | 중간 |
| M3.8 | **FTUE·설정·접근성·세이브 안전 + 텔레메트리 수집** | 옵션, 시계조작 방어, 롤링 백업, D_total 텔레메트리 *수집* 코드(scope C4) | 중간 |
| M3.9 | **출시 폴리시 + QA + 밸런스 패스** | 버그·진행막힘·세이브 손상 점검, 페이싱 재검증 (qa-balancer) | 중간~큼 |

- **순서 근거:** 콘텐츠 확장(M3.1~5)이 본체 → 폴리시(M3.6)가 그 위에 → 패키징/인프라(M3.7~8)는 콘텐츠와 병렬 가능(platform 레이어 격리, tech-arch §5.2) → 최종 QA(M3.9)가 마감. **재하강(M3.3)은 빅 크런치(M3.2) 의존.**
- **컷 안전판:** 시간이 부족하면 M3.5 연구 D가지 후반 노드부터 출시 후로(scope C3). M3.3 재하강은 1회만 확실히 돌아도 출시 가능(2회차는 라이브 보강 가능하나 권장은 1~2회).
- **텔레메트리 주의 (scope C4 / risk R8):** holo_factor 재튜닝은 출시 후지만, **수집 코드(M3.8)는 v1.0 IN.** 안 넣으면 출시 후에도 데이터가 없어 영영 보정 불가.

### 3-B. 졸업 기준 (= 출시 가능)
1. 첫날 첫 상전이 → ~1주 첫 빅 크런치 → 재하강 1~2회로 "수 주" 진입까지 4필러가 한 번도 끊기지 않는다.
2. 6 미지 메커니즘이 각각 "새롭다" (필러 ④ 끝까지).
3. 도감 87 완성 가능, 홀로그래픽 배율이 가드레일(≤×1.35) 안.
4. 세이브가 안전하다 (손상 복구·마이그레이션·백업 작동 — tech-arch §1).
5. Steam 빌드가 오버레이·클라우드·업적과 함께 돈다.
6. qa-balancer 출시 전 체크리스트 통과 (launch-plan §출시 전 체크리스트).

### 3-C. 의존성
- **선행:** 정거장 2 졸업(폴리시 파이프라인 확립). 정거장 2의 아트/사운드 방향을 6층 전체로 확장하는 것이 M3.6.
- **병렬:** 위시리스트 캠페인(launch-plan)은 정거장 2 GIF 확보 시점부터 *이미 진행 중*이어야 함 → 정거장 3 내내 마케팅과 제작이 병행.

---

## 4. 정거장 4 — 출시 후 (Post-launch) ("라이브로 키운다")

> **목표:** 출시한 게임을 *넓혀* 리텐션·재방문 동기를 만든다. scope-mvp §5 OUT 목록을 라이브 콘텐츠로.

### 4-A. 우선순위 (출시 후 데이터 보고 조정)

| 순위 | 항목 | 규모 | 근거 |
|---|---|---|---|
| 1 | **재하강 심화 콘텐츠 (~18입자+~12노드)** | 큼 | scope C1. 회차마다 다른 깊이 보상. 가장 큰 라이브 콘텐츠 |
| 1 | **D_total 텔레메트리 기반 holo_factor 재튜닝** | 작음 | scope C4 / risk R8. 실데이터 도착 후 1.x 패치 밸런스 보정 |
| 2 | **3회차+ 재하강 차별화 / 후기 회차 변별** | 중간 | scope C2. 텔레메트리로 권태 지점 확인 후 튜닝 |
| 2 | **연구 트리 심화 가지 (D후반·B일부)** | 중간 | scope C3. 컬렉터·하드코어용 |
| 가변 | **GDD §13 백로그 "하고 싶음" 항목** | 가변 | 반응 보고 선별 |

### 4-B. 의존성
- **선행:** v1.0 출시 + 텔레메트리 데이터 수집(M3.8). 라이브 튜닝은 실플레이어 데이터 없이 불가.
- **조기경보 연동:** risk-register의 리텐션·번아웃 신호를 보고 라이브 우선순위 조정.

---

## 5. 한눈에 — 정거장 × 규모 × 졸업 요약

| 정거장 | 증명/제공 | 총 규모 | 졸업 한 줄 | 다음으로 가는 조건 |
|---|---|---|---|---|
| **1. 프로토타입** | 코어 루프 재미 | 중간 (코어가 70%) | "한 판 더 하고 싶다" | 졸업 기준 5개, 특히 3·5 |
| **2. 수직 슬라이스** | 보이고 들리는 완성도 | 중간 | "스토어 GIF가 나온다" | GIF 3~5개 + FTUE 명료 |
| **3. v1.0 Steam** | 끝까지 작동하는 완성 게임 | 큼 | "4필러 끊김 없음 + 출시 품질" | 출시 전 체크리스트 통과 |
| **4. 출시 후** | 라이브로 넓힘 | 가변 | (반복) | — |

---

## 6. 소규모·1인 팀을 위한 현실 조언 (문외한용)

- **순서를 지켜라.** 정거장 1을 건너뛰고 콘텐츠(정거장 3)부터 만들고 싶은 유혹이 가장 크고 가장 위험하다. 재미를 모르는 채 87입자를 만들면 다 버릴 수 있다.
- **F-게이트(1일차 고정)는 타협하지 마라.** "나중에 제대로" = 세이브를 깨고 다시 만든다는 뜻. 이건 인디가 흔히 겪는 가장 비싼 후회다.
- **막히면 레인을 바꿔라.** 코어가 안 풀리면 도감 데이터·플레이버를, 그것도 막히면 휴식을. 번아웃(risk-register)이 1인 개발 최대 적.
- **데이터 주도를 믿어라.** 수치는 코드가 아니라 `data/`에 있다(tech-arch §4.4). 밸런스 조정에 코드를 안 건드린다.
- **"더"보다 "더 잘".** 컷은 패배가 아니라 출시 전략이다. 작게 내고 라이브로 키운다.

---

## 7. 한 줄 요약

> **로드맵 = 4개의 정거장: 작게 만들어 재미를 증명하고(1) → 한 구간을 다듬어 팔릴지 보이고(2) → 전체를 완성해 출시하고(3) → 라이브로 키운다(4). 정거장 1의 F-게이트(세이브 봉투·StorageAdapter·고정 timestep·break_eternity)는 1일차에 박고, 각 정거장의 졸업 기준을 통과해야만 다음으로 간다.**
